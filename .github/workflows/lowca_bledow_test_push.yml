name: Lowca Bledow - test push

on:
  workflow_dispatch:

jobs:
  send-test-push:
    runs-on: ubuntu-latest

    steps:
      - name: Send test push via OneSignal
        env:
          ONESIGNAL_APP_ID: ${{ secrets.ONESIGNAL_APP_ID }}
          ONESIGNAL_API_KEY: ${{ secrets.ONESIGNAL_API_KEY }}
        run: |
          curl -sS -X POST "https://api.onesignal.com/notifications" \
            -H "Content-Type: application/json" \
            -H "Authorization: Key ${ONESIGNAL_API_KEY}" \
            -d '{
              "app_id": "'"${ONESIGNAL_APP_ID}"'",
              "target_channel": "push",
              "included_segments": ["Subscribed Users"],
              "headings": {"en": "LOWCA BLEDOW — TEST"},
              "contents": {"en": "Automatyczny test: OneSignal -> telefon -> zegarek."}
            }'
